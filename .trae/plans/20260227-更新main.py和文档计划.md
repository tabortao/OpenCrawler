# 任务计划：更新 main.py 和相关文档

## 任务目标
1. 更新 main.py 文件，彻底废弃旧的 API 端点，直接使用最新的 API
2. 根据 CLAUDE.md 更新 README.md 和 docs/需求文档.md 文件

## 任务步骤

### 1. 更新 main.py 文件
- 移除所有已标记为废弃的 API 端点
- 保留最新的 API 端点和核心功能
- 确保服务正常启动和运行

### 2. 更新 README.md 文件
- 添加 BROWSER_HEADLESS 环境变量配置
- 确保文档与最新的 API 端点保持一致
- 检查并更新其他相关信息

### 3. 更新 docs/需求文档.md 文件
- 更新项目概述，添加少数派平台支持
- 更新技术栈，移除 Crawlee，只保留 Playwright
- 更新平台深度优化表格，添加少数派和通用网站
- 更新接口规范，使用最新的 API 端点
- 更新环境变量配置，添加 BROWSER_HEADLESS
- 更新目录结构，使用最新的项目结构

### 4. 验证更新结果
- 确保 main.py 文件不再包含旧的 API 端点
- 确保 README.md 和 docs/需求文档.md 与 CLAUDE.md 保持一致
- 确保所有文件格式正确，内容完整

## 预期结果
- main.py 文件只包含最新的 API 端点
- README.md 和 docs/需求文档.md 与 CLAUDE.md 保持一致
- 所有文件内容完整，格式正确，便于开发者参考